name: Sync Design System

on:
  push:
    branches:
      - main
      - master
    paths:
      - 'index.html'
  workflow_dispatch:
    inputs:
      force_sync:
        description: '強制同步（即使沒有變更）'
        required: false
        default: 'false'
        type: boolean

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run sync script
        id: sync
        run: |
          node scripts/sync-design-system.js

          # 檢查是否有變更
          if git diff --quiet design-system/; then
            echo "has_changes=false" >> $GITHUB_OUTPUT
            echo "📝 沒有偵測到變更"
          else
            echo "has_changes=true" >> $GITHUB_OUTPUT
            echo "📝 偵測到 design-system 變更"
          fi

      - name: Commit and push changes
        if: steps.sync.outputs.has_changes == 'true' || github.event.inputs.force_sync == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          git add design-system/

          # 取得版本號
          VERSION=$(grep -oP 'APP_VERSION\s*=\s*"v\K[^"]+' index.html || echo "unknown")

          git commit -m "chore: 自動同步 design-system (v${VERSION})" \
                     -m "由 GitHub Actions 自動生成" \
                     -m "觸發: ${{ github.event_name }}" || echo "沒有需要提交的變更"

          git push

      - name: Create summary
        run: |
          echo "## 🎨 Design System 同步結果" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ -f design-system/SYNC_REPORT.md ]; then
            cat design-system/SYNC_REPORT.md >> $GITHUB_STEP_SUMMARY
          fi

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "✅ 同步完成於 $(date)" >> $GITHUB_STEP_SUMMARY
